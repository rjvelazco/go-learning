## learning-go

This repo is a sandbox for building **multiple small Go programs** (mini-apps) to learn the language.

### Structure

- **`cmd/`**: runnable apps (each folder builds to a binary)
- **`domain/`**: reusable packages (core logic)
